# Patterns

Patterns are versioned conceptual solutions under `patterns/`. A frontend pattern
records the problem, interaction, states, motion, accessibility, reference names and
trade-offs. It generalizes product/library-specific implementation and does not serve
as a snippet dump.

