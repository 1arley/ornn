# Detectors

Detectors are optional deterministic checks, separate from skills. Run `ornn detect`
without an API key to emit file, line, rule and message evidence. Signals such as
missing alt attributes or removed focus outlines still require contextual review to
exclude false positives.

