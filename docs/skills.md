# Skills

A skill is a portable `skills/<domain>/<name>/SKILL.md` directory. It must remain useful
when copied alone to a compatible agent. It teaches how to think, investigate, verify,
consult references and avoid false positives. It must not require Ornn runtime state or
invoke a mandatory workflow. Cross-skill composition belongs in recipes.

