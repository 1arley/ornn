# Collections

Collections are versioned discovery and installation bundles. `ornn install motion`
resolves its skill identifiers and installs only that set. Collections do not duplicate
skill content.

