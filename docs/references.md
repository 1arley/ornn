# References

`references/*.yaml` is the URL source of truth. Each entry records name, URL, evidence
type, category, authority, verification date, status, use/avoid guidance and search
queries. Skills name relevant catalogs or sources but do not duplicate URLs.

Types distinguish methodology, heuristic, inspiration, implementation and discovery.

